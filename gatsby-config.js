module.exports = {
  plugins: [
    `gatsby-plugin-image`,
    `gatsby-plugin-sharp`,
    `gatsby-plugin-react-helmet`,
    {
      resolve: `gatsby-plugin-typescript`,
    },
    `gatsby-plugin-sass`,
  ],
  siteMetadata: {
    title: `Clerkent`,
  },
}
